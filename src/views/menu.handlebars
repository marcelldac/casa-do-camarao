<!DOCTYPE html>
<html lang="pt-br">
<div class="container">
  <header class="d-flex justify-content-center py-3">
    <ul class="nav nav-pills">
      <li class="nav-item">
        <a href="/" class="nav-link">Início</a>
      </li>
      <li class="nav-item">
        <a href="/menu" class="nav-link active">Nosso Cardápio</a>
      </li>
      {{#if logged}}
        <li class="nav-item">
          <a href="/logout" class="nav-link">Logout</a>
        </li>
        <li class="nav-item">
          <a href="/create-dish" class="nav-link">Criar Prato</a>
        </li>
      {{else}}
        <li class="nav-item">
          <a href="/login" class="nav-link">Login</a>
        </li>
        <li class="nav-item">
          <a href="/register" class="nav-link">Cadastre-se</a>
        </li>
      {{/if}}
    </ul>
  </header>
</div>
<div class="container mt-4">
  <div class="row row-cols-1 row-cols-md-3 g-4">
    {{#each dishes as |dish|}}
      <div class="col">
        <div class="card h-100 shadow-sm">
          <img src="{{dish.imageUrl}}" class="card-img-top" alt="{{dish.title}}" style="object-fit: cover; height: 200px;">
          <div class="card-body">
            <h5 class="card-title">{{dish.title}}</h5>
            <p class="card-text">{{dish.description}}</p>
          </div>
          <div class="card-footer">
            <small class="text-muted">Cardápio de Inverno ❄️ </small>
          </div>
        </div>
      </div>
    {{/each}}
  </div>
</div>